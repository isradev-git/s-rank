<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        ponytail: allowBackup="false" a propósito. La copia automática de
        Android se llevaría el token de sesión a Google Drive. El techo es
        que el usuario pierde la sesión al cambiar de móvil o reinstalar,
        pero se recupera entrando otra vez, que cuesta menos que ese riesgo.
    -->
    <application
        android:name=".Aplicacion"
        android:allowBackup="false"
        android:label="S-RANK"
        android:supportsRtl="false"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
